import 'package:flutter/material.dart';

/// Techy dark palette for AR + coding contexts.
class AppColors {
  const AppColors._();

  /// Main brand blue (geeky, vibrant).
  static const Color primary = Color(0xFF0FA3FF);
  static const Color primaryGlow = Color(0xFF0C8CE9);

  /// Deep background for dark mode.
  static const Color background = Color(0xFF0B0F1A);

  /// Slightly elevated surface for cards/panels.
  static const Color surface = Color(0xFF121A2A);
  static const Color surfaceMuted = Color(0xFF1A2336);
  static const Color surfaceLight = Color(0xFFF6F8FB);

  /// Accent for code highlights and emphasis.
  static const Color accent = Color(0xFF7C4DFF);

  /// Text colors with high contrast on dark.
  static const Color textPrimary = Color(0xFFE9EDF5);
  static const Color textSecondary = Color(0xFFB7C4DA);
  static const Color textMuted = Color(0xFF7D8AA5);

  /// Borders and strokes.
  static const Color border = Color(0xFF1F2A3C);
  static const Color overlay = Color(0x3323304A);
}

